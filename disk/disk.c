#include "disk.h"

// Reads n sectors from the disk at the sector specified.
char *read_disk(uint16_t sector, uint16_t n)
{
    return 0;
}

// Writes the data specified to the disk at the specified sector.
void write_disk(uint16_t sector, char *content)
{

}